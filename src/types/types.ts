export interface UserType {
    id?: number;
    user?: string;
    email: string;
    password: string;
}

export interface RegisterUserType {
    id?: number;
    user: string;
    email: string;
    password: string;
}